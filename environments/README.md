# Verified environment information

This directory contains only environment information that has been directly
verified from the analysis setup.

## R

Exact versions reported from the working R environment are stored in:

```text
r_package_versions.tsv
```

The recorded R version is `4.5.3`.

Unverified/reconstructed SAMap, pySCENIC, and OrthoFinder environment files are
intentionally not included in this version of the repository.
